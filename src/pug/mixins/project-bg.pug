mixin project-bg(img, title, text)
    // top-project-card
    .project-card
        .project-card__img
            img(src="./img/templates/" + img + ".webp", srcset="./img/templates/" + img + "@2x.webp 2x", alt="project", loading="lazy")
        .project-card__content
            h3.top-project__title !{title}
            p.top-project__text !{text}