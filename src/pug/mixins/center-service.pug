mixin center-service(img, title, hours, price)
    // center-service-card
    .center-service-card
        .center-service-card__img
            svg(class = "svg-" + img)
                use(href="./img/spriteSvg.svg#svg-" + img)
        h3.center-service-card__title !{title}
        ul.center-service-card__list
            li
                span.icon-wrapper
                    svg.svg-clock-white
                        use(href="./img/spriteSvg.svg#svg-clock-white")
                span !{hours}
            li
                span.icon-wrapper &#8381;
                span !{price}
        a(href="#!").center-service-card__link Подробнее